import React from 'react';
import styled from '@emotion/styled';

const Root = styled.div``;

interface Props {}

export const StyledRootTemplate = (props: Props) => {
  return <Root>StyledRootTemplate</Root>;
};
