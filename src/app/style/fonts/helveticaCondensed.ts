import localFont from 'next/font/local';

const helveticaCondensed = localFont({
  src: './HelveticaCondensedRegular.ttf',
  variable: '--font-helvetica-condensed',
});

export default helveticaCondensed;
