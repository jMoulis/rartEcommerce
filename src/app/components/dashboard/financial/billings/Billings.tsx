import { IProductService } from '@/src/types/DBTypes';
import React from 'react';

interface Props {
  products?: IProductService[];
}

export const Billings = (props: Props) => {
  return <div>Billings</div>;
};
