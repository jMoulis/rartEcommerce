import { IProductService } from '@/src/types/DBTypes';
import React from 'react';

interface Props {
  products?: IProductService[];
}

export const Estimates = (props: Props) => {
  return <div>Estimates</div>;
};
